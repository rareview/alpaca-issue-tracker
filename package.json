{
  "name": "alpaca",
  "scripts": {
    "icons:generate": "node scripts/generate-icons.js",
    "prestart": "npm run icons:generate",
    "start": "parcel src/index.jsx",
    "prebuild": "npm run icons:generate",
    "build": "parcel build src/index.jsx",
    "prewatch": "npm run icons:generate",
    "watch": "parcel watch src/index.jsx",
    "release:create": "node scripts/create-release.js",
    "zip": "npm run lint && npm run build && node scripts/plugin-package.mjs",
    "lint": "npm run lint:js && npm run lint:styles && npm run lint:php",
    "prelint:js": "npm run icons:generate",
    "lint:js": "eslint src scripts tests playwright.config.js --ext .jsx,.js,.mjs",
    "lint:styles": "stylelint src/**/*.scss",
    "lint:php": "vendor/bin/phpcs --ignore=src/*",
    "format": "npm run format:js && npm run format:php",
    "format:js": "prettier --write 'src/**/*.{jsx,js,scss}' 'scripts/**/*.{js,mjs}' 'tests/**/*.{js,mjs,json,md,php}' 'playwright.config.js'",
    "format:php": "vendor/bin/phpcbf --ignore=src/*",
    "fix": "npm run fix:js && npm run fix:styles && npm run fix:php",
    "fix:js": "eslint src scripts tests playwright.config.js --ext .jsx,.js,.mjs --fix",
    "fix:styles": "stylelint src/**/*.scss --fix",
    "fix:php": "vendor/bin/phpcbf --ignore=src/*",
    "clear": "rm -rf ./.parcel-cache/ ./dist/",
    "clb": "npm run clear && npm run lint && npm run build",
    "test:e2e:prepare": "npm run zip && node tests/e2e/scripts/build-blueprint.mjs",
    "test:e2e": "npm run test:e2e:prepare && playwright test",
    "test:e2e:install": "playwright install chromium",
    "test:e2e:playground": "node tests/e2e/scripts/start-playground.mjs"
  },
  "devDependencies": {
    "@parcel/transformer-inline-string": "^2.15.4",
    "@parcel/transformer-sass": "^2.15.4",
    "@playwright/test": "^1.59.1",
    "@wordpress/eslint-plugin": "^17.0.0",
    "@wp-playground/cli": "^3.1.20",
    "eslint": "^8.57.0",
    "eslint-plugin-react": "^7.33.2",
    "parcel": "^2.15.4",
    "prettier": "^3.1.1",
    "stylelint": "^16.3.1",
    "stylelint-config-standard-scss": "^13.0.0"
  },
  "alias": {
    "react": "./src/shim/react.js",
    "react-dom": "./src/shim/react-dom.js"
  },
  "dependencies": {
    "dompurify": "^3.3.2",
    "marked": "^16.2.0",
    "prismjs": "^1.30.0",
    "prop-types": "^15.8.1"
  }
}
